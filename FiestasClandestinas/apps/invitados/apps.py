from django.apps import AppConfig


class InvitadosConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.invitados"
    label = "apps_invitados"
    verbose_name = "Frontend Invitados (SSR)"
