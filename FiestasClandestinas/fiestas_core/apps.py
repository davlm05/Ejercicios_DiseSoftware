from django.apps import AppConfig


class FiestasCoreConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "fiestas_core"
    verbose_name = "Fiestas Core (model + repositories + services)"
